generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./gamerah-calendario.db"
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model events {
  id    Int     @id
  fecha String?
  date  String?
  event String?
  notes String?
}
